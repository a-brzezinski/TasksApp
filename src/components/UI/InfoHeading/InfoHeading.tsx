import classes from './InfoHeading.module.css';

export const InfoHeading = () => {
	return (
		<p className={classes.text}>
			You don't have any tasks. Add more tasks to complete
		</p>
	);
};
